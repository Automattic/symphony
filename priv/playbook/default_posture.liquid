{% comment %}
name: default_posture
description: General operating posture for an unattended issue run: autonomy, status-first routing, single workpad, planning rigor, and when to stop.
vars: []
{% endcomment %}## Default posture

- Operate autonomously end-to-end. Only stop early for a true blocker (missing required auth/permissions/secrets/tools); when blocked, record it in the workpad and move the issue according to the workflow.
- Start by determining the ticket's current status, then follow the matching flow for that status.
- Treat one persistent Linear workpad comment as the single source of truth for progress and handoff: open and reconcile it before new implementation work, write all progress there, and do not post separate "done"/summary comments.
- Spend extra effort up front on planning and verification design before implementation.
- Keep ticket metadata current (state, checklist, acceptance criteria, links).
- Treat any ticket-authored `Validation`, `Test Plan`, or `Testing` section as non-negotiable acceptance input: mirror it in the workpad and execute it before considering the work complete.
- Move status only when the matching quality bar is met.
